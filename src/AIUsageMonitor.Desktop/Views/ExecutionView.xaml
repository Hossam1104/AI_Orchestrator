<UserControl
    x:Class="AIUsageMonitor.Desktop.Views.ExecutionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <Style x:Key="ExecutionInputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceMutedBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontFamily" Value="{DynamicResource BrandBodyFontFamily}" />
            <Setter Property="FontSize" Value="13" />
        </Style>
    </UserControl.Resources>
    <ScrollViewer Padding="28" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Style="{StaticResource WorkspacePageStackStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="EXECUTION" Style="{StaticResource EyebrowTextStyle}" />
                    <TextBlock Text="Run approved project work" Margin="0,8,0,0" Style="{StaticResource DisplayTitleStyle}" FontSize="28" />
                    <TextBlock Text="APO uses the selected project context, prepares the existing durable authorities, and keeps every next step evidence-backed." Margin="0,8,0,0" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                </StackPanel>
                <Border Grid.Column="1" Style="{StaticResource StatusChipStyle}" VerticalAlignment="Top" Padding="12,7">
                    <TextBlock Text="{Binding StateText}" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" AutomationProperties.Name="Execution state" />
                </Border>
            </Grid>

            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.25*" MinWidth="360" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="1*" MinWidth="300" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="OWNER MODE" Style="{StaticResource EyebrowTextStyle}" />
                        <TextBlock Text="Start from known context" Margin="0,6,0,0" Style="{StaticResource SectionHeadingStyle}" />
                        <TextBlock Text="PROJECT" Margin="0,18,0,5" Style="{StaticResource EyebrowTextStyle}" />
                        <ComboBox ItemsSource="{Binding ProjectOptions}" SelectedItem="{Binding SelectedProject, Mode=TwoWay}" DisplayMemberPath="DisplayText" Style="{StaticResource BrandComboBoxStyle}" AutomationProperties.Name="Execution project selector" />
                        <Border Margin="0,14,0,0" Padding="12" Background="{DynamicResource SurfaceMutedBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource ControlCornerRadius}">
                            <StackPanel>
                                <TextBlock Text="CONTEXT APO WILL USE" Style="{StaticResource EyebrowTextStyle}" />
                                <TextBlock Text="{Binding SelectedProject.Name, TargetNullValue=Select a registered project}" Margin="0,6,0,0" Style="{StaticResource SectionHeadingStyle}" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding SelectedProject.RepositoryContextText, TargetNullValue=Repository context unavailable}" Margin="0,8,0,0" Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding SelectedProject.TrackerContextText, TargetNullValue=Tracker context unavailable}" Margin="0,3,0,0" Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding SelectedProject.GovernanceContextText, TargetNullValue=Governance context unavailable}" Margin="0,3,0,0" Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding ContextSourceText}" Margin="0,8,0,0" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                                <TextBlock Text="{Binding CurrentWorkText}" Margin="0,4,0,0" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                            </StackPanel>
                        </Border>
                        <TextBlock Text="WORK / REQUEST" Margin="0,16,0,5" Style="{StaticResource EyebrowTextStyle}" />
                        <TextBox Text="{Binding Objective, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ExecutionInputTextBoxStyle}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="72" VerticalScrollBarVisibility="Auto" AutomationProperties.Name="Owner work request" />
                        <TextBlock Text="One concise request is enough when the current context supplies the rest. APO derives a title from this request unless you override it below." Margin="0,6,0,0" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding TitleSourceText}" Margin="0,4,0,0" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock Text="ACCEPTANCE CRITERIA (REQUIRED, ONE PER LINE)" Margin="0,16,0,5" Style="{StaticResource EyebrowTextStyle}" />
                        <TextBox Text="{Binding AcceptanceCriteria, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ExecutionInputTextBoxStyle}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="86" VerticalScrollBarVisibility="Auto" AutomationProperties.Name="Bounded request acceptance criteria" />
                        <TextBlock Text="{Binding MissingContextText}" Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{DynamicResource BrandWarningBrush}" Style="{StaticResource CaptionTextStyle}" AutomationProperties.Name="Missing execution context" />
                        <Expander Header="Advanced details" IsExpanded="False" Margin="0,16,0,0" Style="{StaticResource TechnicalDetailsExpanderStyle}" AutomationProperties.Name="Advanced execution contract details">
                            <StackPanel Margin="12,0,12,14">
                                <TextBlock Text="TITLE" Margin="0,2,0,5" Style="{StaticResource EyebrowTextStyle}" />
                                <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ExecutionInputTextBoxStyle}" AutomationProperties.Name="Bounded request title" />
                                <TextBlock Text="WORK ITEM REFERENCE (OPTIONAL)" Margin="0,14,0,5" Style="{StaticResource EyebrowTextStyle}" />
                                <TextBox Text="{Binding WorkItemReference, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ExecutionInputTextBoxStyle}" AutomationProperties.Name="Optional work item reference" />
                                <TextBlock Text="CONSTRAINTS (OPTIONAL, ONE PER LINE)" Margin="0,14,0,5" Style="{StaticResource EyebrowTextStyle}" />
                                <TextBox Text="{Binding Constraints, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ExecutionInputTextBoxStyle}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="64" VerticalScrollBarVisibility="Auto" AutomationProperties.Name="Bounded request constraints" />
                                <TextBlock Text="VALIDATION EXPECTATIONS (OPTIONAL, ONE PER LINE)" Margin="0,14,0,5" Style="{StaticResource EyebrowTextStyle}" />
                                <TextBox Text="{Binding ValidationExpectations, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ExecutionInputTextBoxStyle}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="64" VerticalScrollBarVisibility="Auto" AutomationProperties.Name="Bounded request validation expectations" />
                            </StackPanel>
                        </Expander>
                        <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                            <Button Content="Prepare" Command="{Binding PrepareCommand}" AutomationProperties.Name="Prepare bounded execution" Style="{StaticResource PrimaryButtonStyle}" ToolTip="{Binding PrepareBlockedReason}" />
                            <Button Content="Start" Command="{Binding StartCommand}" AutomationProperties.Name="Start bounded execution" Margin="8,0,0,0" Style="{StaticResource SecondaryButtonStyle}" />
                            <Button Content="Cancel" Command="{Binding CancelCommand}" AutomationProperties.Name="Cancel bounded execution" Margin="8,0,0,0" Style="{StaticResource GhostButtonStyle}" />
                        </StackPanel>
                        <TextBlock Text="{Binding PrepareBlockedReason}" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="{DynamicResource TextMutedBrush}" Style="{StaticResource CaptionTextStyle}" AutomationProperties.Name="Prepare readiness" AutomationProperties.HelpText="{Binding PrepareBlockedReason}" />
                        <TextBlock Text="{Binding ErrorMessage}" Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{DynamicResource BrandErrorBrush}" AutomationProperties.Name="Execution error" />
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="2">
                    <Border Style="{StaticResource CardStyle}" Padding="20">
                        <StackPanel>
                            <TextBlock Text="ROUTING" Style="{StaticResource EyebrowTextStyle}" />
                            <TextBlock Text="Resolved authority" Margin="0,6,0,0" Style="{StaticResource SectionHeadingStyle}" />
                            <TextBlock Text="Planner" Margin="0,16,0,3" Style="{StaticResource EyebrowTextStyle}" />
                            <TextBlock Text="{Binding PlannerText}" Style="{StaticResource BodyTextStyle}" />
                            <TextBlock Text="Executor" Margin="0,12,0,3" Style="{StaticResource EyebrowTextStyle}" />
                            <TextBlock Text="{Binding ExecutorText}" Style="{StaticResource BodyTextStyle}" />
                            <TextBlock Text="Reason codes" Margin="0,12,0,3" Style="{StaticResource EyebrowTextStyle}" />
                            <TextBlock Text="{Binding RouteReasonText}" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                        </StackPanel>
                    </Border>
                    <Border Margin="0,16,0,0" Style="{StaticResource CardStyle}" Padding="20">
                        <StackPanel>
                            <TextBlock Text="RUN EVIDENCE" Style="{StaticResource EyebrowTextStyle}" />
                            <TextBlock Text="Durable preparation" Margin="0,6,0,0" Style="{StaticResource SectionHeadingStyle}" />
                            <TextBlock Text="Run ID" Margin="0,16,0,3" Style="{StaticResource EyebrowTextStyle}" />
                            <TextBlock Text="{Binding RunIdText}" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                            <TextBlock Text="Workspace" Margin="0,12,0,3" Style="{StaticResource EyebrowTextStyle}" />
                            <TextBlock Text="{Binding WorkspaceText}" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                            <TextBlock Text="Checkpoint" Margin="0,12,0,3" Style="{StaticResource EyebrowTextStyle}" />
                            <TextBlock Text="{Binding CheckpointText}" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                            <TextBlock Text="Terminal result" Margin="0,12,0,3" Style="{StaticResource EyebrowTextStyle}" />
                            <TextBlock Text="{Binding TerminalResultText}" Style="{StaticResource BodyTextStyle}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding AdapterSummaryText}" Margin="0,6,0,0" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                            <TextBlock Text="{Binding ValidationStateText}" Margin="0,14,0,0" TextWrapping="Wrap" Foreground="{DynamicResource BrandWarningBrush}" Style="{StaticResource CaptionTextStyle}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <Border Margin="0,18,0,0" Style="{StaticResource CardStyle}" Padding="20" Visibility="{Binding ShowEmptyState, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="NO REGISTERED PROJECTS" Style="{StaticResource EyebrowTextStyle}" />
                    <TextBlock Text="Add an existing project before preparing execution." Margin="0,7,0,0" Style="{StaticResource SectionHeadingStyle}" />
                    <TextBlock Text="Execution will not create project identity or guess a local workspace." Margin="0,7,0,0" TextWrapping="Wrap" Style="{StaticResource CaptionTextStyle}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
